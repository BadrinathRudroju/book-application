# book-application

Tech stack used - java , springboot, h2 database.

This project consists of complete crud which shows -

create( storing or adding a book in database)
read( can display the book details)
update( can overwrite exitising book details)
delete( delete a book details)

It consists of different packages and classes

Entity - it has a class named Book which acts as a layout for the database, maps a table in it and each entry becomes a row in table
